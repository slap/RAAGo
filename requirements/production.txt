# Dependencias de produccion.
-r base.txt

# WSGI server
gunicorn>=22.0

# Static files
whitenoise>=6.7

# Email: SMTP estandar viene con Django. Para Mailgun u otros proveedores,
# agregar django-anymail cuando se configure el server.
# django-anymail[mailgun]>=11.0

# Object storage (S3) - opcional, descomentar cuando se defina el almacenamiento:
# django-storages[boto3]>=1.14
